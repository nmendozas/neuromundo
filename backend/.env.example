# ─── Servidor ───
PORT=4000
NODE_ENV=development
DB_PATH=./data/neuromundo.db

# ─── Seguridad / Sesiones ───
SESSION_SECRET=cambiar-por-una-frase-larga-y-aleatoria
TOKEN_TTL_DAYS=7

# ─── Admin inicial (se crea en el primer arranque si no existe) ───
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CambiarEstaClave123!

# ─── Correo SMTP (self-hosted, NO es una API de terceros) ───
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=NeuroMundo S.A.S <gerencianeuromundo@gmail.com>

# ─── CORS (origen del frontend; * en desarrollo) ───
CORS_ORIGIN=*
